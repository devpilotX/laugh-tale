# LaughTail SMP - Specification Index

This is the **working entry point** to the specification. `MASTER.md` in this directory is the archive; do not load it. It is 3,994 lines and roughly 300,000 characters, and loading it destroys recall for exactly the details you need (spec 33.4).

**Load only the rows you need for the task in front of you.** Every file below is a self-contained section of the master document.

## Provenance

| Fact | Value |
| --- | --- |
| Source | `MASTER.md`, version 6.0 FINAL, written 2026-08-24 |
| Master line count | 3,994 |
| Part files | 43 (42 sections and appendices, plus `_preamble.md` and `_end-of-document.md`) |
| Headings indexed | 280 (43 top-level, 237 subsections) |
| Split verified | Losslessly, by `scripts/verify-split.ps1`: part line counts sum to 3,994; all 42 `##` headings appear exactly once; parts reconcatenate byte-for-byte identical to `MASTER.md` |
| Regenerate with | `scripts/split-spec.ps1`, then `scripts/verify-split.ps1`, then `scripts/build-index.ps1` |

Document order is `_preamble`, sections 0-26, appendices A-G, sections 27-33, `_end`. The appendices sit physically between sections 26 and 27 in the master; that is not a mistake in the split.

## Quick routes: which files to load for which job

| If you are working on | Load these |
| --- | --- |
| Anything at all, first | `00`, `02` (the ten laws), `23` (decisions already made) |
| Anything that takes money | `03` (legal, non-negotiable), `18`, `24.1` |
| Server bring-up, Docker, portability | `05`, `29`, `30`, `C`, `E` |
| Performance, tuning, the watchdog | `06`, `A`, `31.7` |
| Worlds, claims, resource reset | `07`, `31.12` |
| Berries, shop, auction, order book | `08`, `10`, `D`, `31.5`, `31.10` |
| Rank, Elo, seasons, the Champion | `09`, `B`, `31.1`, `31.2`, `31.3` |
| Cosmetics and particles | `11`, `31.7` |
| War events and the Finale | `12`, `31.2` |
| Voice chat | `13`, `31.11` |
| Rules, punishment, anti-cheat | `14`, `03.5.1`, `31.9` |
| Homes, teleports, quality of life | `15`, `16`, `31.6` |
| Permissions and the Owner/Admin split | `17`, `31.13` |
| Website, store, leaderboards | `18`, `29.11`, `29.12` |
| Any command | `19` |
| Planning or sequencing work | `20`, `21`, `F` |
| Migrating to a bigger VPS | `22`, `05.1` |
| Deciding whether something is in scope | `25`, `26`, `G` |
| How to work as an agent on this repo | `27`, `28`, `32` |
| Bootstrapping from nothing | `33` |

## Cross-cutting warning

Section 31 is a **completeness pass**: it retro-fits values and rules into gaps left by earlier sections. Section 33.1 **supersedes** parts of 29.13 and 32.3. When an earlier section and a later one disagree on the same fact, check `docs/questions.md` first - the known conflicts are recorded there rather than silently resolved.

## Full index

